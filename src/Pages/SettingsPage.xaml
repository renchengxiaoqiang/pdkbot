<ui:Page x:Class="PdkBot.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
      xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
      xmlns:local="clr-namespace:PdkBot.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SettingsPage">
    <Grid>
    <ikw:SimpleStackPanel Spacing="4" Margin="20">
        <TextBlock Text="设置" FontSize="30" Margin="0 20"/>
            <ui:SettingsCard x:Name="cardAppTheme"
                    Description="选择一个你喜欢的主题."
                    Header="主题" IsEnabled="True">
                <ui:SettingsCard.HeaderIcon>
                    <ui:FontIcon Glyph="&#xE793;"/>
                </ui:SettingsCard.HeaderIcon>
                <ComboBox Name="cmbTheme" SelectionChanged="cmbTheme_SelectionChanged">
                    <ComboBoxItem>深色</ComboBoxItem>
                    <ComboBoxItem>浅色</ComboBoxItem>
                </ComboBox>
            </ui:SettingsCard>

            <!--<ui:SettingsCard x:Name="cardAssistPanel"
                Description="设置右侧辅助面板的隐藏和显示."
                Header="辅助面板" IsEnabled="True">
                        <ui:SettingsCard.HeaderIcon>
                            <ui:FontIcon Glyph="&#xE90d;"/>
                        </ui:SettingsCard.HeaderIcon>
                <ui:ToggleSwitch x:Name="tsPanelHidden" IsOn="False" OffContent="隐藏" OnContent="显示" Toggled="tsPanelHidden_Toggled" />
            </ui:SettingsCard>-->

            <!--<ui:SettingsCard Header="A card with custom objects as its Description"
        IsEnabled="True">
            <ui:SettingsCard.Description>
                <ui:HyperlinkButton Content="Learn more about Inkways" />
            </ui:SettingsCard.Description>
            <Button Content="Open Inkways Editor" Style="{StaticResource {x:Static ui:ThemeKeys.AccentButtonStyleKey}}" />
        </ui:SettingsCard>

        <ui:SettingsCard Description="When resizing a SettingsCard, the Content will wrap vertically. You can override this breakpoint by setting the SettingsCardWrapThreshold resource. For edge cases, you can also hide the icon by setting SettingsCardWrapNoIconThreshold."
        Header="Adaptive layouts" IsEnabled="True">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Glyph="&#xE745;"/>
            </ui:SettingsCard.HeaderIcon>

            <Button Content="This control will wrap vertically!"/>
        </ui:SettingsCard>

        <ui:SettingsCard Header="This is a card with a Header only" 
        IsEnabled="True" />-->
    </ikw:SimpleStackPanel>
    </Grid>
</ui:Page>
